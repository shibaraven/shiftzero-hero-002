# Reference simulator service

The deterministic reference world is implemented in `shiftzero.simulator`. It is synthetic,
reproducible, and safe to publish. It must never be labeled as a physical run.

