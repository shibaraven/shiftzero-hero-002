# Three-minute demo video shot list

Target duration: 2:45–2:55. Capture only after the public URL and repository revision are frozen.

## 0:00–0:20 — Problem and promise

- Show the title card: “One sentence in. One verified robot mission out.”
- State the operational risk: an LLM must not directly actuate a warehouse robot.
- Show the Judge Mode `VERIFIED REPLAY` and `LIVE GATE LOCKED` labels in the same frame.

## 0:20–0:50 — Intent to verified proposal

- Enter or read the hero intent: move `P-104` from `INBOUND-01` to `RACK-A12`.
- Run the hero replay through `INTENT → OBSERVED → PLANNED → PROPOSED → VERIFIED`.
- Point out typed tools, frozen schema, deterministic rules, and the human approval binding.

## 0:50–1:35 — Physical-AI failure path

- Start execution and inject the C-04 aisle obstruction.
- Hold on `BLOCKED → SAFE_STOP`; explain that stop authority is local and independent of cloud
  or LLM latency.
- Show the alternate route and `REPLANNING → VERIFIED → EXECUTING`.
- Complete the delivery and show the sealed outcome.

## 1:35–2:05 — Proof and evaluation

- Open Evidence and show `100/100`, zero safety violations, 100% unsafe-condition recall, and
  the six scenario families.
- Download the Evidence Bundle and briefly show its manifest and SHA-256 inventory.
- State clearly that these are reference-simulator results.

## 2:05–2:35 — Architecture and Nebius

- Open Architecture and trace the five layers from Nemotron proposals to the local edge stop.
- After an API key is available, replace this placeholder with a recorded live forced tool call,
  provider/model receipt, and official Compatibility Gate result.
- Do not record or expose the API key.

## 2:35–2:55 — Close

- Return to the completed mission view.
- Close with: “AI proposes. Rules authorize. Humans remain accountable.”
- Show repository and public Judge Mode URLs.

## Required capture checklist

- [ ] 1080p or higher, cursor visible, browser zoom readable.
- [ ] Public URL works in an anonymous/private window.
- [ ] No secrets, local usernames, private endpoints, or customer data are visible.
- [ ] Live Nebius footage is captured only after the official gate passes.
- [ ] Physical AGV language is omitted unless corresponding physical footage and safety approval
      exist.
