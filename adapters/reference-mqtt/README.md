# Reference MQTT adapter

This public boundary will translate an approved mission into synthetic VDA5050-shaped order and
state messages. The current executable reference adapter is in `shiftzero.adapters` and does not
connect to a broker.

