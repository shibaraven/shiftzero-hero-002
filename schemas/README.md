# Frozen contracts

Pydantic is the executable source of truth for typed data contracts. Regenerate the schema files
with `python -m shiftzero.cli export-schemas`. State-machine, safety-policy, and tool-catalog JSON
files are reviewed policy artifacts and must be changed together with code and tests.

